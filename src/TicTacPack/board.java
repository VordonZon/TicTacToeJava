package TicTacPack;

import java.util.HashMap;
import java.util.Map;

public interface board {
	private board;
}

private Map<Integer,Character> makeBoard() {
	private Map<Integer,Character> board = new HashMap<Integer,Character>()
			
	for(int i = 1 ; i <= 9; i++) {
		board.put(i, null);
	}
	return board;
}

